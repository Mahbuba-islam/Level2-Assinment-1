
## Question: What is the use of the keyof keyword in Typescript ?


keyof হলো Typescript এর একটি operator. যা কোনো অবজেক্ট টাইপের সব কী (key) গুলোর একটি ইউনিয়ন টাইপ তৈরি করে। 
অর্থাৎ, অবজেক্টের যেসব প্রপার্টি আছে, তাদের নামগুলোকে একত্র করে একটি টাইপ বানায়।

 উদাহরণ
type Person = {
  name: string;
  age: number;
  country: string;
};

type PersonKeys = keyof Person;

এখানে PersonKeys টাইপটি হবে "name" | "age" | "country" — অর্থাৎ Person টাইপের সব কী গুলোর একটি তালিকা।

 ## ব্যবহার
function getValue(obj: Person, key: keyof Person) {
  return obj[key];
}

const person: Person = {
  name: 'Sara',
  age: ২৫,
  country: 'USA'
};

const value = getValue(person, 'age'); // ২৫

## Why is keyof useful?

1। ভুল key ব্যবহার করলে ypeScript কম্পাইল করার সময়েই ত্রুটি দেখাবে। 

২। অবজেক্টের প্রপার্টি অ্যাক্সেস করা যায় নিরাপদভাবে, রানটাইম ত্রুটি ছাড়াই।

৩ পুনঃব্যবহারযোগ্য ফাংশন বানানো যায়:
একই ফাংশন বা ইউটিলিটি অনেক অবজেক্ট টাইপের জন্য ব্যবহার করা যায়, শুধু keyof দিয়ে টাইপ নিরাপদ করে নিলে।


---------------------------------------------------------------------------------------------------------------------------------------------
---------------------------------------------------------------------------------------------------------------------------------------------

## Question : Explain the difference between any, unknown, and never types in typescript.
## Ans : 
any টাইপ ব্যবহার করলে TypeScript টাইপ চেকিং বন্ধ করে দেয়। এর মানে হলো, 
এই টাইপে যেকোনো মান রাখা যায় এবং যেকোনোভাবে ব্যবহার করা যায়।
ব্যবহার:
যখন টাইপ চেকিং দরকার নেই, তখন any ব্যবহার করা যায়।
উদাহরণ:
let data: any = ৫;
data = "hello";
data = true;

🔹 unknown টাইপ

unknown হলো any এর টাইপ-সেফ সংস্করণ। এতে যেকোনো মান রাখা গেলেও, ব্যবহার করার আগে টাইপ যাচাই করতে হয়।
ব্যবহার:
"যখন মানের ধরন (টাইপ) জানা নেই, কিন্তু নিরাপদভাবে ব্যবহার করতে চাই টাইপ যাচাইয়ের মাধ্যমে।"
উদাহরণ:

if (typeof value === "string") {
  console.log(value.toUpperCase()); 
}

#unknown টাইপ ব্যবহার করলে TypeScript জোর করে টাইপ যাচাই করতে বাধ্য করে, ফলে কোড আরও নিরাপদ হয়।

🔹 never টাইপ
never এমন টাইপ যা কখনোই কোনো মান ধারণ করে না। এটি বোঝায় যে কোনো কোড ব্লক কখনোই কোনো মান রিটার্ন করবে না।
ব্যবহার:
- ফাংশন যদি কখনো কিছু রিটার্ন না করে (যেমন: সবসময় error ছুঁড়ে দেয়)

উদাহরণ:
function throwError(message: string): never {
  throw new Error(message);
}



